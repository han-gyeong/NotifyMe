server:
  port: 0

spring:
  r2dbc:
    url: r2dbc:pool:mysql://dev-mysql:3306/notifyme
    username: notifyme
    password: gksrud12

eureka:
  client:
    service-url:
      defaultZone: http://notification-eureka:8761/eureka/ # Eureka 서버 주소
  instance:
    prefer-ip-address: true # 호스트 이름보다 IP 주소를 우선 사용 (권장)