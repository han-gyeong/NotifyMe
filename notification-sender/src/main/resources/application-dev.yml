server:
  port: 0

spring:
  kafka:
    bootstrap-servers: infra.internal:9092

eureka:
  client:
    service-url:
      defaultZone: http://eureka.internal:8761/eureka/
  instance:
    prefer-ip-address: true